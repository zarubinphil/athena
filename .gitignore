# Секреты — никогда в git
.env
.env.*
*secret*
*credential*
*.pem
id_rsa*

# Артефакты
node_modules/
dist/
build/
runs/
.firecrawl/
__pycache__/
*.log
.DS_Store

# Карта секретов (если положена в проект)
**/references/secrets-map.md

# Athena OS
*.local
# Личные манифесты — реальные живут в приватном overlay, не в generic-репо
projects.manifest
tools.manifest
.secrets/
secrets/
*.env
!*.env.example
chezmoi/**/private_*
.skills-snapshot.tar.zst
smoke/*.log
